<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vici.vici">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/title_activity_maps"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".Activities.MapsActivity" />

        <activity android:name=".Activities.ExisitingUserActivity"
            android:noHistory="true"/>

        <activity android:name=".Activities.SearchpageActivity"/>

        <activity android:name=".Activities.BrowseActivity"/>

        <activity android:name=".Activities.BrowsePageTest"/>

        <activity android:name=".Activities.VAPActivity"/>

        <activity android:name=".Activities.MapsViewActivity" />

        <activity android:name=".Activities.NewUserActiviy"
            android:windowSoftInputMode="adjustPan"/>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-6205064581462054~6251431045"/>

        <meta-data
            android:name="com.here.sdk.access_key_id"
            android:value="ARjh8JA6eaDrVblDqbnOiQ" />
        <meta-data
            android:name="com.here.sdk.access_key_secret"
            android:value="N102tjvdJRHfbii3qckLfgoB9tMsZiVCPpNdqXhwfuHXrnq3pWBfERS16aBWXFl5TCC26POCuzWaBka2KIPkzg" />

    </application>

</manifest>